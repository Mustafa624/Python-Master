# كيفية تشغيل الموقع محلياً

## ⚠️ ملاحظة مهمة

**Pyodide يحتاج اتصال بالإنترنت للعمل!**

حتى لو كان الموقع على جهازك، Pyodide يحتاج تحميل المكتبة من CDN عبر الإنترنت.

## 🚀 طرق تشغيل الموقع

### الطريقة 1: استخدام Live Server (الأسهل)

1. **في VS Code:**
   - ثبت إضافة "Live Server"
   - اضغط بزر الماوس الأيمن على `index.html`
   - اختر "Open with Live Server"

2. **في Cursor:**
   - نفس الخطوات كما في VS Code

### الطريقة 2: استخدام Python HTTP Server

1. افتح Terminal/Command Prompt في مجلد المشروع
2. اكتب:
   ```bash
   python -m http.server 8000
   ```
3. افتح المتصفح واذهب إلى: `http://localhost:8000`

### الطريقة 3: استخدام Node.js HTTP Server

1. ثبت `http-server`:
   ```bash
   npm install -g http-server
   ```
2. في مجلد المشروع:
   ```bash
   http-server
   ```
3. افتح المتصفح واذهب إلى الرابط المعروض

### الطريقة 4: استخدام PHP Server

1. في مجلد المشروع:
   ```bash
   php -S localhost:8000
   ```
2. افتح المتصفح واذهب إلى: `http://localhost:8000`

## ✅ بعد التشغيل

- الموقع سيعمل بشكل طبيعي
- Pyodide سيتم تحميله من CDN عبر الإنترنت
- محرر الكود سيعمل بشكل كامل

## 🔧 استكشاف الأخطاء

### المشكلة: Pyodide لا يعمل
**الحل:**
- تأكد من وجود اتصال بالإنترنت
- تأكد من أنك تستخدم سيرفر محلي (ليس file://)
- جرب تحديث الصفحة (F5)

### المشكلة: الموقع لا يفتح
**الحل:**
- تأكد من أنك في المجلد الصحيح
- تأكد من أن المنفذ (port) غير مستخدم
- جرب منفذ آخر (8001, 8080, إلخ)

## 📝 ملاحظات

- **لا تفتح الملف مباشرة** (file://) - هذا لن يعمل مع Pyodide
- **يجب استخدام سيرفر محلي** حتى لو كان الموقع على جهازك
- **Pyodide يحتاج الإنترنت** لتحميل المكتبة (حوالي 10-15 ثانية في أول استخدام)

## 🌐 النشر على الإنترنت

عندما تنشر الموقع على الإنترنت:
- Pyodide سيعمل تلقائياً
- لا تحتاج أي إعدادات إضافية
- فقط تأكد من أن جميع الملفات موجودة

